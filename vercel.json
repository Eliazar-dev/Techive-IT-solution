{
  "installCommand": "npm install",
  "buildCommand": "npm run build:shared && npm run build:web",
  "outputDirectory": "apps/web/dist",
  "functions": {
    "api/index.ts": {
      "includeFiles": "apps/api/**"
    }
  }
}
